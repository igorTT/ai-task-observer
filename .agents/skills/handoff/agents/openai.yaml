interface:
  display_name: "Handoff"
  short_description: "Start a titled task in this worktree"
  default_prompt: "Use $handoff to create a new titled task in this worktree."

policy:
  allow_implicit_invocation: true
