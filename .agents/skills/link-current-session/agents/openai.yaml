interface:
  display_name: "Link Matching Sessions"
  short_description: "Link title-matching sessions to a Linear issue"
  default_prompt: "Use $link-current-session to link every session with ENG-215 in its title to ENG-215."

policy:
  allow_implicit_invocation: true
